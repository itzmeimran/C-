#include <iostream>
#include <vector>

using namespace std;

int main(){
    vector<int>arr = {5,1,3,9,10,12,6,2};

    for (int i=arr.size()-1;i>=0;i--){
        for (int j=i+1;i<arr.size();j++){
            if(arr)
        }
    }
}